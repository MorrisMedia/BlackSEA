version: '3.8'

services:
  db:
    image: postgis/postgis:16-3.4
    container_name: bsdip-db
    environment:
      POSTGRES_DB: bsdip
      POSTGRES_USER: bsdip
      POSTGRES_PASSWORD: bsdip_dev_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bsdip -d bsdip"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
